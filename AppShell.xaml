<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiProject.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiProject.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="MauiProject">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <ShellContent
        Title="ListView"
        ContentTemplate="{DataTemplate local:ListViewPage}"
        Route="ListView" />
    <ShellContent
        Title="ListView"
        ContentTemplate="{DataTemplate local:AddImagePage}"
        Route="AddImage" />
    <ShellContent
        Title="ListView"
        ContentTemplate="{DataTemplate local:DeleteImagePage}"
        Route="DeleteImage" />
    <ShellContent
        Title="Tietoja"
        ContentTemplate="{DataTemplate local:AboutPage}"
        Route="About" />

</Shell>
